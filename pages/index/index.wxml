<!--index.wxml-->
<view class="maingrid">
  <view wx:for="{{showArray}}" wx:for-index="idx" wx:key="idx">

    <CardItem src="{{item.src}}" status="{{item.status}}" itemid="{{item.id}}" idx="{{idx}}" data-index="{{idx}}" bind:myclick="testChildClick"></CardItem>

  </view>
  </view>