<!--index.wxml-->
<view class="container">
  <view wx:for="{{showArray}}" wx:for-index="idx" wx:key="idx">
    <CardItem src="{{item}}" bindtap="parentClick" data-idx="{{idx}}"></CardItem>
  </view>

</view>